// Código para que aparezcan rellenados los campos de zona empleados en la interfaz del responsable con lo que haya en la base de datos

using System;
using UnityEngine;
using UnityEngine.Networking;
using System.Collections;
using TMPro;
using UnityEngine.UI;

public class EmpleadosDataBase : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
