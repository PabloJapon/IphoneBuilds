using UnityEngine;
using TMPro;
using UnityEngine.UI;

public class ChangeFontCliente : MonoBehaviour
{
    // public TMP_Text textMeshPro; // Reference to the TextMeshPro component
 
    public TMP_Dropdown dropdown; // Reference to the TMP_Dropdown component
    public TMP_FontAsset newFont; // The new font to assign
    public TMP_FontAsset newFont2;
    public TMP_FontAsset newFont3;

  //  void Start()
  //  {
        // Subscribe to the dropdown's value changed event
  //      dropdown.onValueChanged.AddListener(delegate {
   //         DropdownValueChanged(dropdown);
   //     });
  //  }

   // void DropdownValueChanged(TMP_Dropdown dropdown)
  //  {
        // Check if the selected option is the first option
   //     if (dropdown.value == 0)
  //      {
            // Change font if the first option is selected
    //        ChangeTextFont(newFont);
   //     }
  //      if (dropdown.value == 1)
 //       {
            // Change font if the first option is selected
   //         ChangeTextFont(newFont2);
  //      }
 //       if (dropdown.value == 2)
 //       {
            // Change font if the first option is selected
 //            ChangeTextFont(newFont3);
 //       }
 //   }

  //  void ChangeTextFont(TMP_FontAsset font)
  //  {
   //     TMP_Text[] tmpTextComponents = Resources.FindObjectsOfTypeAll<TMP_Text>();
  //      foreach (TMP_Text tmpTextComponent in tmpTextComponents)
  //      {
  //          tmpTextComponent.font = font;
  //      }
 //
 //   }
}
